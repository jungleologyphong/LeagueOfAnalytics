import React from 'react';
import './styles.css';
const Header = () => {
  return (
    <header className="containerHeader">
      <button>Login</button>
      <button>Register</button>
    </header>
  );
};

export default Header;